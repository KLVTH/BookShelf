import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "gray",
    height: 52,
    width: "100%",
    marginBottom: 16,
    borderRadius: 12,
  },
})